# Acceptance Contract

This skill ships when it can:

- create a bounded coding brief
- invoke a Codex-compatible harness through an adapter
- capture changed files
- capture commands run
- capture verification results
- return a structured handoff

It must never push or open a PR without approval.
