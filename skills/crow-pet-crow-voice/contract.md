# Acceptance Contract

This skill ships when it can:

- compress agent replies without losing technical meaning
- select `lite`, `full`, or `ultra` based on context
- preserve code, schema, file paths, commands, URLs, and quoted errors
- switch to normal prose for safety, approvals, or ambiguity
- stop when the user requests normal mode
- stay runtime-neutral and adapter-safe
