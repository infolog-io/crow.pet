# crow.pet Orchestrator

The orchestrator receives tasks, enforces policy, selects adapters, launches specialists, and reports status.

It is not tied to OpenClaw, Codex, Claude Desktop, or any single runtime.

Runtime-specific behavior belongs in `../../adapters`.
